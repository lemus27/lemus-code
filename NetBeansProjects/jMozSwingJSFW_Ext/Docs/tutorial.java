//tutorial d e manejo de  MozSwing
/**
 *para  que  funcione  debes   agregar a las  lib todos los paquete  que tiene
 * y debes  agregar las  carpetas de  xulruner a la carpeta del proyecto
 *-- native
 *-- extensions
 *-- update
 * el jar  no  funciona por si solo necesita  toda la carpeta dist pero detener tambien las  carpetas
 * -- native
 *-- extensions
 *-- update
 * @author mike
 *   //con el metodo load de la clase  MozillaWindow tenemos ponemos la ruta  para  cargar el  archivo html, que deberia ser el index.
 * MozillaWindow win = new MozillaWindow();
 * win.setSize(500, 600);     
 * win.load("C:\\Documents and Settings\\mike\\Mis documentos\\NetBeansProjects\\jMozSwingVentana\\ExtDesigner\\designer.html");
 * win.setVisible(true);
 */