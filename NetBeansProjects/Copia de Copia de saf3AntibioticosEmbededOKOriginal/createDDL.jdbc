CREATE TABLE APP.ANTIBIOTICOS (IDANTIBIOTICOS BIGINT NOT NULL, NOMBREPRESCRIPTOR CLOB(64000), FECHA DATE, CEDULA BIGINT, LOTE VARCHAR(15), DOMICILIO CLOB(64000), CANTDESECHADA BIGINT, OBSERVACIONES CLOB(64000), NUMFACTURA BIGINT, STOCK BIGINT, FECHACADUCIDAD DATE, CANTADQUIRIDA BIGINT, NOMBRE CLOB(64000), PRIMARY KEY (IDANTIBIOTICOS))
